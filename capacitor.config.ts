import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'broken-shared-header',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
